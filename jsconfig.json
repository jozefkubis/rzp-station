{
  "compilerOptions": {
    "ignoreDeprecations": "6.0",
    "paths": {
      "@/*": ["./*"] // ← alias "@/…" → koreň
    }
  },
  "include": ["**/*"] // (nepovinné, ale užitočné pre IntelliSense)
}
